# =============================================================================
# MASSEURMATCH - Environment Variables Template
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to git - it's already in .gitignore
# =============================================================================

# -----------------------------------------------------------------------------
# SUPABASE CONFIGURATION (Required)
# -----------------------------------------------------------------------------
# Get these values from: https://app.supabase.com/project/_/settings/api

# Your Supabase project URL (e.g., https://xxxxx.supabase.co)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Your Supabase anonymous/public key (safe to expose in browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase service role key (NEVER expose in browser, server-side only)
# Used for: admin operations, seed scripts, backend API
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# -----------------------------------------------------------------------------
# SITE CONFIGURATION
# -----------------------------------------------------------------------------

# Your site URL (used for OAuth redirects, password reset emails, etc.)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Set to "true" to prevent search engine indexing (staging/dev environments)
NEXT_PUBLIC_NO_INDEX=false

# -----------------------------------------------------------------------------
# STRIPE PAYMENT INTEGRATION (Optional)
# -----------------------------------------------------------------------------

# Stripe API endpoint or backend URL for payment processing
NEXT_PUBLIC_STRIPE_BACKEND=https://your-stripe-backend.com/api
# Alternative names (the app checks multiple env var names):
# NEXT_PUBLIC_STRIPE_BACKEND_URL=
# NEXT_PUBLIC_STRIPE_API=

# -----------------------------------------------------------------------------
# IDENTITY VERIFICATION (Optional)
# -----------------------------------------------------------------------------

# Set to "false" to disable ID verification requirement during signup
NEXT_PUBLIC_REQUIRE_ID_VERIFICATION=true

# -----------------------------------------------------------------------------
# BACKEND API (Optional)
# -----------------------------------------------------------------------------

# Backend API URL for admin operations and AI features
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000

# -----------------------------------------------------------------------------
# AI INTEGRATION - DeepSeek API (Optional)
# -----------------------------------------------------------------------------

# DeepSeek API key for AI chat features (KnottyBot)
# Get from: https://platform.deepseek.com/api-keys
DEEPSEEK_API_KEY=your-deepseek-api-key-here

# -----------------------------------------------------------------------------
# BACKEND SERVER CONFIGURATION (ia-backend/)
# -----------------------------------------------------------------------------

# Port for the backend Express server (default: 4000)
PORT=4000

# Supabase URL for backend (can be same as NEXT_PUBLIC_SUPABASE_URL)
SUPABASE_URL=https://your-project.supabase.co

# =============================================================================
# QUICK START GUIDE
# =============================================================================
#
# MINIMAL SETUP (Just to run the app):
# 1. Copy this file: cp .env.example .env.local
# 2. Fill in these required variables:
#    - NEXT_PUBLIC_SUPABASE_URL
#    - NEXT_PUBLIC_SUPABASE_ANON_KEY
# 3. Run: npm install && npm run dev
#
# FULL SETUP (All features):
# 1. Set up Supabase project at https://supabase.com
# 2. Fill in all SUPABASE_* variables
# 3. (Optional) Set up Stripe for payments
# 4. (Optional) Set up DeepSeek for AI chat
# 5. Run backend: cd ia-backend && npm install && npm start
#
# PRODUCTION DEPLOYMENT:
# - Set NEXT_PUBLIC_NO_INDEX=false
# - Set NEXT_PUBLIC_SITE_URL to your production domain
# - Enable NEXT_PUBLIC_REQUIRE_ID_VERIFICATION=true
# - Never expose SUPABASE_SERVICE_ROLE_KEY to the browser
#
# =============================================================================
